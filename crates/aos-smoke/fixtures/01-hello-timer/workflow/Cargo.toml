[workspace]

[package]
name = "hello-timer-workflow"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
aos-wasm-sdk = { path = "../../../../aos-wasm-sdk" }
serde = { version = "1", default-features = false, features = ["derive", "alloc"] }
serde_cbor = { version = "0.11", default-features = false, features = ["alloc"] }
