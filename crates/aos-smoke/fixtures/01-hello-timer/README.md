# Example 01 — Hello Timer

- Scope: workflow emits `timer.set`, handles `TimerEvent::Fired` (wrapped `sys/TimerFired@1` receipt).
- Follow the detailed spec in TODO.MD when populating `air/` and the workflow crate.
