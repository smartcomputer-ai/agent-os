//! AIR v1 core types and semantic validation (skeleton)
//! See: spec/03-air.md and spec/10-air-implementation.md

pub mod validate {
    // Placeholder for semantic checks (DAG, references, bindings)
    // Intentionally minimal for initial scaffold.
    pub fn validate_plan_semantics(_name: &str) -> Result<(), String> {
        Ok(())
    }
}

